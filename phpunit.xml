<?xml version="1.0" encoding="UTF-8"?>
<!--
    SIHLA — configuración de PHPUnit / Pest.

    A PROPÓSITO ESTÁ CASI VACÍO (§7.4).

    La configuración de tests vive ENTERA en .env.testing, que se genera
    con `cp env.testing.example .env.testing` y es idéntico en la Mac y
    en CI. Acá solo va APP_ENV, porque es lo que hace que Laravel cargue
    .env.testing durante el bootstrap.

    Duplicar variables entre este archivo y .env.testing garantiza que un
    día se desincronicen y que la suite pruebe algo distinto de lo que
    creemos que prueba.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/pestphp/pest/preset/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         processIsolation="false"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         displayDetailsOnTestsThatTriggerNotices="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         failOnDeprecation="false"
         failOnNotice="true"
         failOnWarning="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Filament/Resources/*/Pages</directory>
        </exclude>
    </source>

    <php>
        <!-- Lo único que va acá: dispara la carga de .env.testing. -->
        <env name="APP_ENV" value="testing"/>
    </php>
</phpunit>
